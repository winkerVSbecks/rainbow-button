const breakpoints = [32, 40, 56, 64].map(n => n + 'em');

const colors = {
  black: '#222',
  white: '#fff',
  orange: '#FBAB33',
  purple: '#B745F6',
  aqua: '#2CF8D2',
  pink: '#F556AB',
  blue: '#3B32F8',
};

const fontSizes = [12, 14, 16, 20, 24, 32, 48, 64, 96];

const space = [0, 4, 8, 16, 32, 64, 128];

const widths = [16, 32, 64, 128, 256];

export default {
  breakpoints,
  colors,
  fontSizes,
  space,
  widths,
};
